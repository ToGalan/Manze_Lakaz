{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Godot (current project)",
            "type": "godot",
            "request": "launch",
            "project": "${workspaceFolder}",
            "scene": "main"
        }
    ]
}
